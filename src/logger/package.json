{
  "name": "logger",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.27"
  },
  "scripts": {
    "build": "esbuild ./index.ts --bundle --format=esm --outfile=dist/logger.js"
  }
}
